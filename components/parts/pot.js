// ============================================================
// Saksı — tüm çiçeklerin altında görünür, kendi bağımsız dosyası.
// Sadece burayı değiştirerek saksının rengini/boyutunu/şeklini
// hiçbir çiçeği etkilemeden değiştirebilirsin.
// ============================================================
const POT = {
  rim: {
    width: 160,
    height: 20,
    radius: 4,
    color: "#C97A32",
    grayColor: "#9c9c9c",
  },

  body: {
    width: 128,
    height: 96,
    color: "#B5651D",
    grayColor: "#8a8a8a",
    bottomLeftRadius: 25,
    bottomRightRadius: 25,
  },
};

export default POT;
